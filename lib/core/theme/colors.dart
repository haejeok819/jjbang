import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFF4EBCE); // 메인
  static const text = Color(0xFF1E1E1E);
  static const subText = Color(0xFF4A4A4A);

  static const card = Colors.white;
  static const accent = Color(0xFF7ED6C1); // 포인트(파스텔 민트)
}
